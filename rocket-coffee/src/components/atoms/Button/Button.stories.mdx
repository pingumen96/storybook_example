import { Meta, Story, Canvas } from "@storybook/addon-docs/blocks";
import Button from "./Button";

<Meta title="atoms/Button" component={Button}>

# Button

This is the **Button** component. The intent of the **Button** component is to provide a button that can be used in a variety of contexts.

<Button onClick={console.log}>Click here</Button>

_This is a button outside of the Storybook canvas_

<Canvas>
  <Story name="Button">
    <Button onClick={console.log}>Click here</Button>
  </Story>
</Canvas>